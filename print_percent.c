#include "main.h"
/**
 * print_percent - prints a percentage sign
 *
 * @c: percentage sign
 * Return: 1 (prints one percentage sign)
 */
int print_percent(void)
{
	int count = 0;
	_putchar ('%');
	count++;

	return (count);
}
